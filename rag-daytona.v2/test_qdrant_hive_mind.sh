#!/bin/bash
# Populate Hive Mind with REAL embeddings
# Redirects to the Python script which handles the logic correctly via Traefik

echo "� Delegating to Python script for real embedding generation..."
python3 /root/populate_hive_mind_real.py